# Porting Backlog — North Mini Code (Cohere2 MoE) → tpu-inference

> Target: `CohereLabs/North-Mini-Code-1.0` · model_type `cohere2_moe` · arch `Cohere2MoeForCausalLM`
> Repo: `chiefkarlin/tpu-inference` (fork of `vllm-project/tpu-inference`), branch `feature/north-mini-code`
> Generated by: Orchestrator (Lead Engineer). Status: **pending human review before heavy delegation.**

---

## 1. Executive Summary

North Mini Code (NMC) is a **30.48B-parameter Mixture-of-Experts** model (3B active per token) tuned for
agentic code generation. It is a Cohere2-MoE architecture: 49 transformer layers, 128 routed experts
(8 active/token), GQA 32:4 attention, **interleaved (NeoX) RoPE**, a **hybrid full/sliding-window
attention schedule**, and a **parallel transformer block** (`x + attn(norm(x)) + mlp(norm(x))`, single
shared RMSNorm, no post-attention norm).

**Key conclusion of the gap analysis: no brand-new Pallas kernels are strictly required.** NMC's
distinguishing features — sigmoid MoE routing with non-renormalized top-k, sliding-window attention,
interleaved RoPE, conditional per-layer RoPE, and GQA — are **already supported** by existing kernels
and layers in `tpu-inference`. The porting effort is therefore dominated by **model-level integration**
(a new `cohere2_moe.py` model file), **weight remapping** (per-expert sharded safetensors → fused 3-D
grouped-GEMM layout), and **v7x performance tuning** of the existing MoE/attention kernels for NMC's
specific shapes.

The closest existing references are **`deepseek_v3.py`** (sigmoid routing + `norm_topk_prob` handling),
**`gpt_oss.py`** (per-layer full/sliding attention schedule via `attention_metadata.sliding_window`), and
**`gemma4.py`/`llama4.py`** (sliding-window + interleaved RoPE via `attention_chunk_size` /
`rope_input_ordering="interleaved"`).

### Effort/risk at a glance

| Area | Status | Owner |
|------|--------|-------|
| Sigmoid MoE routing + non-norm topk | ✅ supported (fused_moe v1, deepseek_v3) | — |
| Sliding-window attention (kernel) | ✅ supported (RPA v3) | — |
| Sliding-window wiring (decode path) | ⚠️ verify + wire (model-level) | Kernel Eng |
| Interleaved RoPE | ✅ supported (`apply_rope`, `rope_input_ordering`) | — |
| Conditional per-layer RoPE | ✅ supported (`use_attention_rope`) | — |
| Parallel transformer block | ❌ new model wiring | Kernel Eng |
| Grouped-GEMM for 128 tiny experts | ✅ kernel exists (megablox); ⚠️ tune for v7x | Perf Eng |
| Weight loading/remapping | ❌ new loader | Kernel Eng |
| Model registration | ❌ new entry in `model_loader.py` | Kernel Eng |
| v7x GKE bring-up + Docker + test | ❌ new | Perf Eng |

---

## 2. Reference Architecture (NMC specifics)

From `config.json` and HF `modeling_cohere2_moe.py`:

- **Layers:** 49. `layer_types = [full, sliding, sliding, sliding]` repeating ×12 (=48) + 1 final `full`.
  - Layer 0 is **dense** (`first_k_dense_replace=1`, `prefix_dense_intermediate_size=3072`).
  - Layers 1–48 are **MoE** (128 experts, `intermediate_size=768` per expert, `num_experts_per_tok=8`).
  - `num_shared_experts=0` → no shared experts.
- **Attention:** GQA, `num_attention_heads=32`, `num_key_value_heads=4` (8:1), `head_dim=128`,
  `attention_bias=false`, `use_qk_norm=false`.
- **Sliding window:** `sliding_window=4096` applied on `sliding` layers; `full` layers attend to all KV.
- **RoPE:** `rope_theta=50000`, `rope_scaling=null`. **Interleaved (NeoX-style)** rotation.
  - **Critical RoPE rule:** RoPE is applied iff `sliding_window is not None OR force_rope`, where
    `force_rope = (mlp_layer_type == dense AND prefix_dense_sliding_window_pattern == 1)`.
    → Sliding layers get RoPE; dense layer 0 gets RoPE (force_rope); **full-attention MoE layers do NOT
    get RoPE.** Must match exactly.
- **MoE routing:** `expert_selection_fn=sigmoid`, `norm_topk_prob=false`. Router = `mlp.gate`
  (128×2048). `topk` on logits → `sigmoid(selected)` → NO renormalization (raw sigmoid scores are the
    expert weights). (topk on logits ≡ topk on sigmoid(logits); selection identical to deepseek_v3.)
- **Experts:** gated SiLU (`use_gated_activation=true`, `hidden_act=silu`):
  `down_proj(silu(gate_proj(x)) * up_proj(x)) * weight`.
- **Norm:** T5-style RMSNorm (variance only, no centering), `rms_norm_eps=1e-6`. Single `input_layernorm`
  per layer (no `post_attention_layernorm`) because of the **parallel block**.
- **Parallel block:** `hidden = x + attn(input_layernorm(x)) + mlp(input_layernorm(x))`.
- **Embeddings / head:** `vocab_size=262144`, `hidden_size=2048`. `lm_head` is **tied** to
  `embed_tokens`. `logit_scale=1.0`. `max_position_embeddings=500000` (256K context / 64K gen).
- **dtype:** bfloat16.

### Weight naming (safetensors index — 49 shards, 30.48B params)

- `model.embed_tokens.weight` (262144×2048) — tied to `lm_head` (no separate `lm_head` key).
- `model.norm.weight` (final RMSNorm).
- Layer 0 (dense): `model.layers.0.{input_layernorm.weight, mlp.{gate,up,down}_proj.weight,
  self_attn.{q,k,v,o}_proj.weight}`.
- Layers 1–48 (MoE): `model.layers.{i}.input_layernorm.weight`;
  `model.layers.{i}.self_attn.{q,k,v,o}_proj.weight`; `model.layers.{i}.mlp.gate.weight` (router);
  `model.layers.{i}.mlp.experts.{e}.{gate,up,down}_proj.weight` for `e in 0..127` — **stored as 128
  separate per-expert matrices** (gate/up = (768,2048), down = (2048,768)), NOT a fused 3-D tensor.

> **Implication:** the weight loader must stack per-expert `gate_proj`+`up_proj` into the fused
> `(128, 2*768, 2048)` layout and `down_proj` into `(128, 2048, 768)` expected by the megablox/fused_moe
> grouped-GEMM kernels.

---

## 3. Gap Analysis vs tpu-inference

### ✅ Already supported (no kernel work)

1. **Sigmoid routing + non-renormalized top-k.** `kernels/fused_moe/v1/kernel.py` has a `sigmoid` case
   and `renormalize_topk_logits: bool = False` (default). `models/jax/deepseek_v3.py` implements
   `scoring_func="sigmoid"` with `norm_topk_prob` gating. `layers/common/fused_moe_gmm.py` has the
   `sigmoid` activation path. NMC's router maps directly onto this.
2. **Sliding-window attention kernel.** `kernels/ragged_paged_attention/v3/kernel.py` (+ `kernel_hd64.py`,
   `tuned_block_sizes.py`) accepts `sliding_window: int | None` with proper masking
   (`q_span < k_span + sliding_window`). The MLA v1/v2 kernels also support it (not needed for GQA).
3. **Interleaved RoPE.** `layers/jax/rope_interface.py::apply_rope(..., rope_input_ordering="interleaved")`
   implements NeoX-style adjacent-pair rotation (vs `"split"` Llama half-split). Used by llama4.
4. **Conditional per-layer RoPE.** `Attention.__call__(..., use_attention_rope: bool = True)`; llama4
   toggles it via `no_rope_layer_interval`.
5. **GQA, RMSNorm, gated SiLU, tied embeddings, bf16.** Standard primitives.

### ⚠️ Supported in kernel but needs model-level wiring / verification

6. **Sliding-window wiring in the decode path.** The base `Attention.attention()`
   (`layers/jax/attention/attention.py`) calls `ragged_paged_attention` **without** forwarding
   `sliding_window`. Two precedents exist: `gpt_oss.py` sets `attention_metadata.sliding_window` per
   layer; `gemma4.py`/`llama4.py` pass `attention_chunk_size=sliding_window` into the `Attention`
   dataclass (used by the prefill path in `attention_interface.py`). **Verify which path reaches the
   RPA v3 kernel in decode for NMC's sliding layers, and wire it consistently.** This is the single
   most important correctness risk.

### ❌ Genuinely new work

7. **Parallel transformer block.** No existing JAX model uses a true parallel block — `gpt_oss`,
   `deepseek_v3`, `gemma4`, `llama4` all use sequential blocks (separate `input_layernorm` +
   `post_attention_layernorm`). NMC uses a single shared `input_layernorm` feeding both attn and mlp.
   Implement in the new model file.
8. **Hybrid layer schedule.** Per-layer: full (sliding_window=None, no RoPE) vs sliding
   (sliding_window=4096, RoPE) vs dense layer 0 (no SW, RoPE forced). Plus dense-vs-MoE MLP per layer.
9. **Weight loader/remapper.** Stack 128 per-expert matrices into fused 3-D; handle dense layer 0;
   tie `lm_head` to `embed_tokens`.
10. **Model registration.** Add `Cohere2MoeForCausalLM` to `_MODEL_REGISTRY` in
    `models/common/model_loader.py` (and decide flax_nnx vs vllm-impl path — see §5).

---

## 4. Kernel Conversion Backlog (Pallas)

> Because the required kernels already exist, this section lists **no new Pallas kernels to author**
> from scratch. Instead it lists kernel **extensions/tuning/verification** items. If the sliding-window
> decode wiring turns out to need a kernel-level change, that becomes the one new-kernel item.

### K-1. Verify & wire sliding-window through the decode attention path *(Kernel Eng, HIGH)*
- Confirm `ragged_paged_attention` v3 receives `sliding_window=4096` for NMC sliding layers in **decode**
  (not just prefill). If the base `Attention.attention()` decode path silently drops it, add the wiring
  (pass `md.sliding_window` / `self.attention_chunk_size` into the `ragged_paged_attention` call).
- Ensure `sliding_window=None` (full attention) for full-attention MoE layers and is handled by the
  kernel's `None` default (no masking).
- Acceptance: prefill + decode attention outputs match a reference for a sliding layer over a >4096
  context.

### K-2. MoE grouped-GEMM (megablox/fused_moe) shape verification *(Kernel Eng, HIGH)*
- NMC MoE shape: 128 experts, 8 active/token, intermediate=768, hidden=2048, gated SiLU, sigmoid routing,
  no renorm. Verify the fused_moe/megablox GMM path (`layers/common/fused_moe_gmm.py`,
  `kernels/megablox/gmm*.py`, `kernels/fused_moe/v1/kernel.py`) accepts this exact topology end-to-end
  with `router_act="sigmoid"`, `renormalize=False`.
- Confirm the fused 3-D weight layout `(E, 2*I, D)` / `(E, D, I)` is what the kernel consumes, and that
  the weight loader produces it.

### K-3. New model file `models/jax/cohere2_moe.py` *(Kernel Eng, HIGH — bulk of work)*
Implement `Cohere2MoeForCausalLM(nnx.Module)` following the `deepseek_v3.py` / `gpt_oss.py` structure:
- `__init__(self, vllm_config, rng, mesh)`; build embed_tokens, 49 decoder layers (dense/MoE +
  full/sliding schedule), final RMSNorm, tied lm_head.
- `__call__(self, kv_caches, input_ids, attention_metadata, ...)`.
- `compute_logits`, `load_weights` (or `WeightLoader`), `initialize_cache` as needed.
- **Parallel block** decoder layer: `h = x + attn(norm(x)) + mlp(norm(x))` (single `input_layernorm`,
  no post-attn norm).
- Per-layer: `use_attention_rope` (True for sliding + dense layer 0; False for full-attention MoE),
  `sliding_window` (4096 for sliding; None otherwise), `rope_input_ordering="interleaved"`,
  `rope_theta=50000`.
- Dense layer 0 MLP: `intermediate_size=3072`. MoE layers: 128 experts × 768.
- `logit_scale=1.0` on tied head.

### K-4. Weight remapping + loader *(Kernel Eng, HIGH)*
- Map `mlp.experts.{e}.{gate,up,down}_proj.weight` → fused `gate_up_proj`/`down_proj` 3-D.
- Map dense layer 0 `mlp.{gate,up,down}_proj` → standard dense MLP weights.
- Tie `lm_head` ← `embed_tokens`.
- Add a `WeightLoader(BaseWeightLoader)` if RunAI streaming is desired (optional); otherwise
  `load_weights`.

### K-5. Register `Cohere2MoeForCausalLM` *(Kernel Eng, MEDIUM)*
- In `models/common/model_loader.py::_get_model_architecture`: import + register
  `_MODEL_REGISTRY["Cohere2MoeForCausalLM"] = Cohere2MoeForCausalLM`.
- Decide impl path: NMC is **not** in `_VLLM_PREFERRED_ARCHITECTURES`, so `auto` → `flax_nnx` (preferred
  for the JAX-native port). Do **not** add it to `_PP_DISABLED_MODELS` unless PP issues surface.

### K-6. Unit tests *(Kernel Eng, MEDIUM)*
- Weight-shape tests (loader produces correct fused layouts).
- Routing correctness (sigmoid, no-renorm) vs a tiny reference.
- Parallel-block forward vs a reference (NumPy/HF) on a 2-layer mini config.
- RoPE: interleaved rotation + conditional application correctness.

### K-7. (Optional) Fused parallel-block kernel *(Kernel Eng/Perf Eng, LOW — stretch)*
- A single fused kernel that computes `x + attn(norm(x)) + mlp(norm(x))` could cut HBM round-trips.
  Defer until correctness + baseline latency are established.

### Performance-tuning kernel items (Performance Eng)

### P-1. fused_moe / megablox tuned_block_sizes for NMC on v7x *(Perf Eng, HIGH)*
- Add/verify tuned block sizes for `(E=128, I=768, D=2048, topk=8)` grouped GEMM on the v7x topology.
  Inspect `kernels/fused_moe/v1/tuned_block_sizes.py` and `kernels/megablox/tuned_block_sizes.py`.

### P-2. RPA v3 tuned_block_sizes for head_dim=128 + sliding_window=4096 *(Perf Eng, HIGH)*
- Verify/tune `kernels/ragged_paged_attention/v3/tuned_block_sizes.py` (and `..._hd64.py` if head_dim
  routing applies) for NMC's GQA 32:4, head_dim=128, sliding_window=4096 on v7x.

### P-3. v7x GKE bring-up, Docker image, end-to-end run *(Perf Eng, HIGH)*
- Auth to `gke_northam-ce-mlai-tpu_us-central1-c_robv-tpu-v7x`; build+push custom Docker image to GAR
  (use repo `docker/` as reference); inject `HF_TOKEN` into the pod to download
  `CohereLabs/North-Mini-Code-1.0` at runtime; submit via kubectl on the `single-host-vllm` node pool
  (`v7x-4` topology).

### P-4. Capture ML Diagnostics trace + latency targets *(Perf Eng, MEDIUM)*
- Profile prefill + decode; report per-kernel timings; identify HBM-bound bottlenecks for the MoE GEMM
  and sliding-window attention. Track against a TBD latency target.

---

## 5. Architecture & Porting Checklist (JAX/Flax)

- [ ] New file `tpu_inference/models/jax/cohere2_moe.py` with `Cohere2MoeForCausalLM`.
- [ ] Parallel-block `DecoderLayer` (shared `input_layernorm`, no post-attn norm).
- [ ] Layer schedule builder: `layer_types` [full,sliding,sliding,sliding]×12 + full; dense layer 0.
- [ ] Attention config per layer: `sliding_window`, `use_attention_rope`, `rope_input_ordering="interleaved"`,
      `rope_theta=50000`, `head_dim=128`, GQA 32:4.
- [ ] MoE config: 128 experts, topk=8, `router_act="sigmoid"`, `renormalize=False`, intermediate=768,
      gated SiLU, no shared experts.
- [ ] Dense MLP for layer 0 (intermediate=3072).
- [ ] Final `JaxRmsNorm` (T5-style, eps=1e-6, no centering); embed_tokens 262144×2048.
- [ ] Tied lm_head = embed_tokens; `logit_scale=1.0`.
- [ ] Weight loader: stack per-expert → fused 3-D; dense layer 0; tie head.
- [ ] Register in `model_loader.py`.
- [ ] `flax_nnx` impl path (default `auto`).

---

## 6. Delegation Strategy

**Branch strategy:** all work on `feature/north-mini-code` (fork `chiefkarlin/tpu-inference`).
Agents work in **git worktrees** off `feature/north-mini-code` to avoid checkout conflicts; the
Orchestrator reviews and merges back.

### Kernel Engineer (worktree A: `../nmc-kernels`)
Primary owner of the **model integration** (K-1 … K-6). Deliverable: a running `Cohere2MoeForCausalLM`
JAX model that loads NMC weights and passes unit tests, merged into `feature/north-mini-code`.
- Inputs provided: this backlog, the HF config/architecture summary, the closest references
  (`deepseek_v3.py`, `gpt_oss.py`, `gemma4.py`, `llama4.py`), the weight-naming table, and the
  accelerator-agents tooling (`MaxCode`, `MaxKernel`).
- Constraints: match the RoPE conditional rule exactly; preserve sigmoid-no-renorm routing; do not
  regress existing models.

### Performance Engineer (worktree B: `../nmc-perf`)
Owner of **v7x bring-up + tuning** (P-1 … P-4). Begins in parallel once the model file lands a first
compilable draft; runs on `gke_northam-ce-mlai-tpu_us-central1-c_robv-tpu-v7x` (`v7x-4`,
`single-host-vllm`). Deliverable: a Docker image pushed to GAR, an end-to-end NMC inference run on v7x,
profiling traces, and tuned block sizes.
- Inputs provided: cluster auth instructions, Docker reference, `HF_TOKEN` injection plan, JAXBench
  tooling, and the kernel tuning targets above.

### Orchestrator (this agent)
- Maintains `PORTING_OVERVIEW.md` + `TASK_LIST.md`.
- Creates worktrees, spawns agents via `scion`, reviews/meres branches, resolves integration conflicts,
  and escalates to the user on significant milestones or blockers.

---

## 7. Testing Plan (v7x)

1. **Compile smoke test:** load NMC config (`load_format=dummy`) and run a single forward in JAX to
   confirm XLA compilation on v7x-4.
2. **Weight-load test:** real weights from HF (via `HF_TOKEN`); verify shapes + a few forward logits
   against HF reference on a short prompt.
3. **Correctness sweep:** sliding-window masking over >4096 context; full-attention layers; RoPE
   presence/absence per layer type; MoE expert selection parity.
4. **Latency profiling:** prefill + decode throughput; per-kernel timings; tune block sizes (P-1/P-2).
5. **Regression:** ensure no existing registered model breaks.

---

## 8. Verification Findings (code-confirmed by Orchestrator against `feature/north-mini-code`)

> The backlog above was spot-checked against the actual repo. Findings below **supersede** any
> conflicting phrasing in §2–§6. The Kernel Engineer MUST read these before starting.

### V1. Sliding-window wiring (K-1) — root cause + solution template CONFIRMED
- **Root cause:** the base `Attention.attention()` decode path (`layers/jax/attention/attention.py`,
  `_ragged_paged_attention` ~L243-250) calls `ragged_paged_attention(...)` **WITHOUT** `sliding_window`.
  Setting `attention_metadata.sliding_window` alone on the base class is **silently dropped in decode**.
- **Prefill path is fine:** `layers/common/attention_interface.py` ~L405 forwards
  `sliding_window=attention_chunk_size`, so `attention_chunk_size=sliding_window` (gemma4 pattern) works
  for prefill.
- **Decode solution template:** `layers/jax/attention/gpt_oss_attention.py` (NOT `models/jax/`) defines a
  custom attention module whose `attention()` calls the RPA kernel with
  `sliding_window=md.sliding_window` (L195). gpt_oss uses `ragged_paged_attention_hd64` because its
  head_dim==64. **NMC head_dim==128 → `use_hd64=False` (attention_interface.py:383 `q.shape[-1]==64`),
  so NMC must call the regular `ragged_paged_attention` (v3), NOT the hd64 variant.**
- **Recommended NMC approach:** create a custom attention module (subclass/mirror of base `Attention`,
  modeled on `gpt_oss_attention.py`) whose decode `attention()` calls
  `ragged_paged_attention(..., sliding_window=md.sliding_window, ...)`. Per layer set
  `attention_metadata.sliding_window = 4096` (sliding layers) or `None` (full layers) — gpt_oss pattern
  (`models/jax/gpt_oss.py:537-538`) — AND set `attention_chunk_size=sliding_window` for the prefill path
  (gemma4 pattern, `models/jax/gemma4.py:525`).

### V2. MoE API names — corrected
- `layers/common/fused_moe_gmm.py::fused_moe_func` uses parameters `scoring_fn` and `activation`
  (**not** `router_act`); `renormalize: bool` is a **required arg with NO default**.
- `router_act="sigmoid"` lives on the **`Router` class** (`models/jax/llama4.py:524`), not in
  `fused_moe_gmm.py`.
- `models/jax/deepseek_v3.py` uses `scoring_func` (default `"sigmoid"`, L1007/1158) + `norm_topk_prob`
  (L864, L1110-1119: `weights_TX /= sum + 1e-20`). For NMC (`norm_topk_prob=false`) this renorm branch
  is skipped → raw sigmoid weights. Map NMC onto this path with `norm_topk_prob=False`.
- `kernels/fused_moe/v1/kernel.py::apply_scoring_fn` has the `sigmoid` case; `renormalize_topk_logits`
  defaults `False` (L95, L1251).

### V3. Model interface contract — corrected (critical for K-3)
- `_validate_model_interface` (`models/common/model_loader.py:691-739`) only *requires* that `__init__`
  accept `vllm_config` and `__call__` accept `kv_caches`, `input_ids`, `attention_metadata` (as kwargs).
- **But the real instantiation** (`_get_nnx_model`, model_loader.py:144) calls
  `model_class(vllm_config, rng, mesh)` — **three positional args**. So the constructor MUST be
  `__init__(self, vllm_config, rng_key, mesh)` (see `DeepseekV3ForCausalLM.__init__` deepseek_v3.py:1376).
  A model with only `__init__(self, vllm_config)` passes validation but **fails at construction**.
- `__call__` signature (see deepseek_v3.py:1407): `__call__(self, kv_caches, input_ids,
  attention_metadata, inputs_embeds=None, ...)`.

### V4. Weight-stacking template — corrected (critical for K-4)
- `DeepseekV3ForCausalLM.load_weights` (deepseek_v3.py:1441) does **NOT** do per-expert stacking — it
  delegates to `JaxAutoWeightsLoader`. Do **not** use it as the stacking template.
- **Use this template instead:** `models/jax/gemma4.py:216-249` (`load_nnx_param_from_reshaped_torch`,
  `gate_up_proj` split into `kernel_gating_EDF`/`kernel_up_proj_EDF`, `down_proj`→`kernel_down_proj_EFD`
  with `permute_dims=(0,2,1)`) and `models/jax/gpt_oss.py:283-294` (mappings) + `358-374` (even=gate /
  odd=up interleaved split → `mlp2_weight_EFD`). Confirm the exact EFD vs EDF layout the megablox/fused_moe
  kernel consumes before finalizing.
- NMC safetensors store **128 separate per-expert** `gate/up=(768,2048)`, `down=(2048,768)` matrices →
  stack into `(128, 2*768, 2048)` and `(128, 2048, 768)`.

### V5. RoPE — default + interleaved style
- `layers/jax/rope_interface.py::apply_rope` defaults `rope_input_ordering="split"` (L50). NMC needs
  NeoX **adjacent-pair** rotation → **explicitly pass `rope_input_ordering="interleaved"`** (handled
  L208-228). The backlog's phrase "NeoX-style adjacent-pair" = the `"interleaved"` branch.
- Conditional per-layer RoPE via `use_attention_rope` confirmed on `Attention.__call__` (attention.py:110)
  and used by llama4 (`models/jax/llama4.py:518,635`).

### V6. Attention attributes are split across files (no single reference)
- No existing model combines all three of: `attention_chunk_size=sliding_window` (gemma4.py:525),
  `rope_input_ordering="interleaved"` (llama4.py:574), conditional `use_attention_rope` (llama4.py:518).
  llama4 uses `attention_chunk_size=None if use_attention_rope else 8192` (a fixed chunk, NOT
  sliding_window). **NMC must combine these deliberately.**

### V7. `layer_types` string values
- HF `config.json` uses `"full_attention"` / `"sliding_attention"` (not `"full"`/`"sliding"`). 49 entries:
  `[full_attention, sliding_attention×3]` repeated ×12 + final `full_attention`. Layer 0 is dense
  (`first_k_dense_replace=1`, `prefix_dense_intermediate_size=3072`); layers 1–48 MoE.

### V8. Architecture numbers re-confirmed from HF config.json
- `num_hidden_layers=49`, `num_experts=128`, `num_experts_per_tok=8`, `num_shared_experts=0`,
  `intermediate_size=768`, `prefix_dense_intermediate_size=3072`, `hidden_size=2048`, `head_dim=128`,
  `num_attention_heads=32`, `num_key_value_heads=4`, `sliding_window=4096`, `rope_theta=50000`,
  `rope_scaling=null`, `expert_selection_fn="sigmoid"`, `norm_topk_prob=false`, `use_gated_activation=true`,
  `use_parallel_block=true`, `use_qk_norm=false`, `attention_bias=false`, `vocab_size=262144`,
  `logit_scale=1.0`, `rms_norm_eps=1e-06` (note: `layer_norm_eps=1e-05` also present but unused for RMSNorm),
  `model_type="cohere2_moe"`, `dtype="bfloat16"`, `max_position_embeddings=500000`.

---

## 9. Performance Engineering Findings (P-1/P-2 Inspection — code-verified)

> Added by Orchestrator after Performance Engineer completed P-1/P-2 source inspection.
> These findings CORRECT assumptions in the original P-1/P-2 brief (section 4).

### P-1 CORRECTED: MoE default path bypasses all tuned_block_sizes tables

**Original assumption (wrong):** "Tune `fused_moe/v1/tuned_block_sizes.py` + `megablox/tuned_block_sizes.py` for NMC MoE."

**Actual dispatch path:** `layers/jax/moe/utils.py:259 select_moe_backend(use_ep)`:
- `USE_MOE_EP_KERNEL=1` + `use_ep=True` → `MoEBackend.FUSED_MOE` (uses `fused_moe/v1/tuned_block_sizes.py`)
- `USE_UNFUSED_MEGABLOCKS=1` → `MoEBackend.MEGABLX_GMM` (uses `megablox/tuned_block_sizes.py`)
- Otherwise → `MoEBackend.GMM_TP` (default for single-host TP-sharded; **NMC's path**)

All env flags default False. For single-host v7x-4 TP-sharded (`use_ep=False`), the default is **GMM_TP** → `fused_moe_func` → `gmm_wrapper` → `gmm_v2` (`kernels/megablox/gmm_v2.py`).

**The three tuned_block_sizes.py files are ALL OFF the default path:**
1. `fused_moe/v1/tuned_block_sizes.py` — only FUSED_MOE backend (needs `USE_MOE_EP_KERNEL=1` + EP)
2. `megablox/tuned_block_sizes.py` (gmm.py) — only MEGABLX_GMM backend (needs `USE_UNFUSED_MEGABLOCKS=1`); AND `gmm_fn` passes explicit tiling bypassing the table anyway
3. `gmm_v2` (actual NMC path) has **NO tuned table** — only `calculate_tiling` (L884-994), a pure VMEM-fitting heuristic

**`gmm_wrapper` (`fused_moe_gmm.py:123`) hardcodes `tile_info=default`.**

**Conclusion:** Editing any `tuned_block_sizes.py` has ZERO effect on NMC's default MoE path. Real tuning lever = wire custom `tile_info` through `gmm_wrapper`→`gmm_v2`, OR switch backend to FUSED_MOE. Both are call-site changes.

**Decision (Orchestrator):** Option (a) — accept GMM_TP heuristic for baseline. Revisit tile_info wiring (option b) post-baseline if profiling proves MoE is a bottleneck.

### P-2 CORRECTED: RPA v3 clamps KV to sliding_window internally — NO over-fetch

**Original assumption (wrong):** "Default ignores sliding_window → 2x HBM over-fetch in decode."

**Actual behavior:** Regular RPA v3 (`kernels/ragged_paged_attention/v3/kernel.py`) CLAMPS KV to `sliding_window` internally:
- Decode (L388-391): `cur_seq_start_bkv_idx = jnp.maximum(kv_q_gap - sliding_window, 0) // bkv_sz`
- Prefill (L939-948): `start_bkv_idx` + `effective_kv_len` clamp to `sliding_window`
- bkv loop is dynamic `pl.loop(unroll=False)` (L956) — no code bloat, per-iter DMA clamped to sw tokens

**Defaults are FUNCTIONALLY CORRECT — no over-fetch.**

**The regular `tuned_block_sizes.py` (4463 lines) is DEAD CODE** — `get_tuned_block_sizes` is never called in `kernel.py` (only `kernel_hd64.py` self-tunes). Verified: grep for `get_tuned_block_sizes` in `kernel.py` returns nothing.

**Real tuning lever = pass explicit `block_sizes` from the NMC custom attention module** (Kernel Engineer owns it):
- Default decode `bkv_csz=min(8192, max_kv)=8192` tokens → ~8MB/buffer × 2 double-buffer = borderline v7x VMEM
- Recommended `bkv_csz=2048-4096` relieves VMEM + improves DMA/compute overlap
- `page_size` likely 16 (`max_model_len=256K > 8192`)

**Decision (Orchestrator):** Kernel Engineer should parameterize `block_sizes` in the custom attention module so we can tune post-baseline without code changes. Performance Engineer provides the `bkv_csz=2048-4096` recommendation.
